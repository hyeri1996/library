<?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.greenart.mapper.BookRankInfoMapper">
    <select id="NewPublicLank" resultType="com.greenart.vo.BookRankInfoVO">
        select distinct
            a.isbn13, a.rank, a.title, a.author
        from book_rank as a 
            inner join public_library as b 
        on a.isbn13 = b.isbn13
            inner join new_book as c 
        on a.isbn13 = c.isbn13 
        where a.rank ='1'
    </select>

    <select id="NewSmallLank" resultType="com.greenart.vo.BookRankInfoVO">
        select distinct 
            a.isbn13, a.rank, a.title, a.author
        from book_rank as a 
            inner join small_library as b 
        on a.isbn13 = b.isbn13
            inner join new_book  as c
        on a.isbn13 = c.isbn13 
        where a.rank ='1'
    </select>

    

</mapper>