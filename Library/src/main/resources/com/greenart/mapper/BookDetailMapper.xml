<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatix.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.greenart.mapper.BookDetailMapper">
    <select id="selectBookDetail" resultType="com.greenart.vo.BookDetailVO">
        select
            title,vol,author,publisher,pub_date
        from book_detail
        where title like #{keyword}
        limit 10 offset #{offset}
    </select>
    <select id="selectBookDetailCnt" resultType="java.lang.Integer">
        select
            count(*)
        from book_detail
        where title like #{keyword}
    </select>

    <select id="selectBookRankChange" resultType="com.greenart.vo.BookRankVO">
    select distinct 
	change_rank,reg_rank,prev_rank,book_title,author,publisher,reg_dt
    from book_rank_ch
    where reg_dt = #{reg_dt}
    </select>
</mapper>